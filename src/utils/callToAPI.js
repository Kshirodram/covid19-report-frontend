/**
 * Implemented fetch API wrapper for better 
 * error handling & response parsing
 *
 * @param {string}  API Url 
 * @param {string}  HTTP method for API call
 * @return {object} Object contains all the body/query/path/header params
 * that are all in the http standard formats
 */
// TODO: to remove API call from actions and put it here
const callToAPI = async (url, method, options) => {
};

export {callToAPI}
